import {
  rawBodySymbol
} from "./chunk-CDXJFGTO.js";

// src/fast-fetch.ts
import { Readable } from "node:stream";
var OriginalResponse = global.Response;
var _a;
var Response = class extends (_a = OriginalResponse, rawBodySymbol, _a) {
  constructor(body, init) {
    super(body, init);
    if (typeof body === "string" || body instanceof Uint8Array || body instanceof ArrayBuffer || body instanceof Readable) {
      this[rawBodySymbol] = body;
    }
  }
};
Object.defineProperty(global, "Response", {
  value: Response
});
